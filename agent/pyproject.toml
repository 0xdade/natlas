[project]
dependencies = [
  "httpx~=0.28",
  "natlas-protocol",
  "playwright~=1.51"
]
name = "natlas-agent"
requires-python = ">=3.13"
version = "0.1.0"

[tool.uv]
package = false

[tool.uv.sources]
natlas-protocol = {workspace = true}
