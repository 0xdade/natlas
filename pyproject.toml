[dependency-groups]
dev = [
  "mypy~=1.14",
  "pytest-cov~=6.0",
  "pytest~=8.3",
  "tach~=0.34"
]

[tool.ruff]

[tool.ruff.lint]
ignore = [
  "PLC0414",
  "PLR0911",
  "PLR0912",
  "PLR0915",
  "PLR2004",
  # TODO: audit all mutable ClassVar annotations and re-enable
  "RUF012"
]
select = [
  # flake8-bugbear
  "B",
  # flake8-comprehensions
  "C4",
  # mccabe complexity
  "C90",
  # pyflakes
  "F",
  # flynt
  "FLY",
  # Furb
  "FURB",
  # isort
  "I",
  # flake8-logging
  "LOG",
  # pylint
  "PL",
  # flake8-use-pathlib
  "PTH",
  # flake8-return
  "RET",
  # Ruff's own rules
  "RUF",
  # flake8-simplify
  "SIM",
  # flake8-tidy-imports
  "TID",
  # pyupgrade
  "UP"
]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"config.settings".msg = "Use django.conf.settings"
"django.contrib.auth.models.User".msg = "Use custom_auth.models.User"

[tool.ruff.lint.mccabe]
max-complexity = 16

[tool.uv]
package = false

[tool.uv.workspace]
members = ["agent", "server"]
